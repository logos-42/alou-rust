[package]
name = "alou_code_commands"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[dependencies]
plugins = { package = "alou_code_plugins", path = "../plugins" }
runtime = { package = "alou_code_runtime", path = "../runtime" }
serde_json = "1"