# Changelog

All notable changes to Alou Desktop will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.2.2] - 2026-03-13

### 🎉 新增功能

#### DIAP 长期存储
- **DIAP 文件管理器** - 新增 `diap_file_manager.rs`，实现 DIAP 身份的本地文件系统持久化
- **启动自动加载** - 应用启动时自动从磁盘加载所有已保存的 DIAP 身份到内存
- **原子写入** - 使用临时文件 + 重命名确保写入安全性

#### 智能体记忆持久化
- **Rust 后端直接写入** - `agent_document` update 操作现在由 Rust 直接写入文件系统
- **正确的 agent_id 传递** - 事件携带正确的 agent_id，解决群聊场景下的路径错误
- **前端缓存优化** - 支持 full(首次) 和 lite(后续) 两种提示词缓存，节省 token

### 🐛 Bug 修复

#### 记忆持久化修复
- **修复写入路径问题** - 群聊时记忆不再写入到群聊频道 ID，而是正确的智能体 ID
- **前端缓存刷新** - 记忆更新后正确清除缓存

### 🔧 技术改进

#### Token 优化
- **按需注入记忆** - 首次激活注入全部7个文档，后续对话只注入 MEMORY.md
- **双版本缓存** - 预生成 full 和 lite 两个版本的系统提示词

---

## [0.2.1] - 2026-03-11

### 🐛 Bug 修复

#### 智能体加载修复
- **修复智能体加载重复问题** - 修复 `loadChannelList` 函数中当频道不存在于列表时未能正确返回更新后列表的 bug
- **防止重复加载** - 添加 `hasLoadedInitialChannelsRef` 防止在组件重新渲染或 session 变化时重复加载初始频道列表

---

## [0.1.10] - 2026-02-28

### 🎉 新增功能

#### 群聊功能增强
- **创建群聊 UI** - 添加群聊创建模态框，支持输入群聊名称创建新群聊
- **加入群聊 UI** - 添加群聊加入模态框，支持输入群聊 ID 加入现有群聊
- **群聊操作按钮** - 在群聊面板头部添加"创建"和"加入"快捷按钮
- **加载状态指示器** - 在头部显示加载动画，提供明确的加载反馈
- **错误提示 Toast** - 消息发送失败时显示红色错误提示框，支持手动关闭

#### 安全性提升
- **XSS 防护** - 集成 DOMPurify，清理消息内容中的恶意 HTML/脚本

### 🐛 Bug 修复

#### 关键修复
- **修复 getActiveAction 调用错误** - 添加 `activeChannelId` 参数，确保正确获取活跃行动
- **修复事件监听器内存泄漏** - 使用空依赖数组，避免重复添加监听器
- **修复消息处理器清理不完整** - 在 `leaveGroup` 时正确清理 service 中的消息处理器
- **优化轮询间隔** - 将 PubSub 轮询间隔从 3 秒改为 1 秒，提升消息实时性

### 🔧 技术改进

#### 性能优化
- 消息轮询间隔优化 (3000ms → 1000ms)
- 事件监听器只添加一次，避免不必要的重新渲染

#### 代码质量
- 移除调试日志依赖
- 统一错误处理机制
- 改进 TypeScript 类型定义

### 📦 依赖更新
- 新增 `dompurify` 用于 XSS 防护

### 📝 文档
- 新增 CHANGELOG.md
- 更新 README.md（群聊功能说明）

---

## [0.1.9] - 2026-02-15

### 新增功能
- 群聊基础功能（IPFS PubSub 消息传递）
- 多智能体协作支持
- 群聊存档列表

### Bug 修复
- 修复群聊按钮显示问题
- 修复消息同步问题

---

## [0.1.8] - 2026-02-01

### 新增功能
- AI 智能体对话系统
- Web3 钱包集成
- MCP 工具系统

---

## 版本说明

### 版本号规则
- **主版本号**：重大架构变更或不兼容更新
- **次版本号**：新功能发布
- **修订号**：Bug 修复和小改进

### 发布周期
- 主版本：不定期发布
- 次版本：每 2-4 周
- 修订版：按需发布

### 符号说明
- 🎉 新增功能
- 🐛 Bug 修复
- 🔧 技术改进
- 📦 依赖更新
- 📝 文档更新
- ⚠️ 重要提示（可能有破坏性变更）
