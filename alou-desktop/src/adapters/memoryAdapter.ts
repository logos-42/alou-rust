/**
 * Memory 模式群聊适配器
 *
 * 注意：核心逻辑在 Rust 后端 (tools/adapters/group_adapter.rs)
 * 这里只是重新导出，保持向后兼容
 *
 * @module adapters/memoryAdapter
 */

export { MemoryGroupChatAdapter } from './groupChatAdapter'
