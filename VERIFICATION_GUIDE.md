# Alou Desktop MVP 0.1.10 验证指南

## 📋 修复内容总结

### P0 关键修复（已完成）
1. ✅ **getActiveAction 调用错误** - `useGroupChatRemoteControl.ts`
2. ✅ **事件监听器内存泄漏** - `useGroupChatRemoteControl.ts`
3. ✅ **消息处理器清理不完整** - `useLocalIpfsGroupChat.ts`
4. ✅ **消息发送失败用户提示** - `GroupChatPanel.jsx`
5. ✅ **轮询间隔优化** - `pubsubService.ts` (3 秒 → 1 秒)

### P1 重要改进（已完成）
6. ✅ **创建群聊 UI 表单** - 模态框 + 按钮
7. ✅ **加入群聊 UI 入口** - 模态框 + 按钮
8. ✅ **错误提示 Toast** - 红色错误提示框
9. ✅ **加载状态指示器** - 头部旋转动画
10. ✅ **群聊面板动画** - CSS 动画优化
11. ✅ **XSS 安全防护** - DOMPurify 集成

---

## 🧪 手动验证步骤

### 1. 启动应用

```bash
cd /Users/apple/Downloads/alou/alou-desktop
npm install          # 安装新依赖 (dompurify)
npm run dev         # 启动开发服务器
```

### 2. 基础功能验证

#### ✅ 群聊按钮测试
- [ ] 群聊按钮可以正常点击
- [ ] 点击后群聊面板打开
- [ ] 面板打开时输入模式自动切换到"群聊"

#### ✅ 创建群聊测试
- [ ] 点击头部"➕ 创建"按钮
- [ ] 弹出创建群聊模态框
- [ ] 输入群聊名称（如"测试群聊"）
- [ ] 点击"创建"按钮
- [ ] 群聊创建成功，显示在存档列表中

#### ✅ 加入群聊测试
- [ ] 点击头部"🚀 加入"按钮
- [ ] 弹出加入群聊模态框
- [ ] 输入群聊 ID
- [ ] 点击"加入"按钮
- [ ] 成功加入群聊

#### ✅ 发送消息测试
- [ ] 在消息输入框输入文本
- [ ] 按 Enter 或点击发送按钮
- [ ] 消息显示在消息列表中
- [ ] 消息成功发送到 IPFS PubSub

#### ✅ 错误提示测试
- [ ] 断开 IPFS 节点（可选）
- [ ] 尝试发送消息
- [ ] 显示红色错误提示框
- [ ] 错误提示有"⚠️"图标
- [ ] 可以点击"×"关闭错误提示

#### ✅ 加载状态测试
- [ ] 触发加载操作（如切换群聊）
- [ ] 头部显示"加载中..."指示器
- [ ] 指示器有旋转动画
- [ ] 加载完成后指示器消失

#### ✅ 消息历史测试
- [ ] 发送多条消息
- [ ] 刷新页面
- [ ] 消息仍然存在（持久化）
- [ ] 可以查看历史消息

#### ✅ 群聊切换测试
- [ ] 创建或加入多个群聊
- [ ] 在存档列表中点击不同群聊
- [ ] 消息列表正确切换
- [ ] 没有消息串群

---

## 🔍 重点验证项目

### 内存泄漏验证
1. 打开浏览器开发者工具 → Memory 标签
2. 创建群聊 → 加入群聊 → 离开群聊
3. 重复 10 次以上
4. 检查内存使用是否稳定（应无持续增长）

### 事件监听器验证
1. 打开浏览器开发者工具 → Console
2. 输入：`getEventListeners(window, 'switch-input-target')`
3. 多次打开/关闭群聊面板
4. 监听器数量应保持为 1（不应持续增长）

### 消息实时性验证
1. 打开两个浏览器窗口
2. 加入同一个群聊
3. 在窗口 A 发送消息
4. 窗口 B 应在 1-2 秒内收到消息

---

## 🐛 已知问题排查

### 问题 1: IPFS 节点不可用
**症状**: 显示"IPFS 节点不可用"错误

**解决**:
```bash
# 检查 IPFS 是否运行
ipfs version

# 如果没有安装，应用会自动下载 Kubo 二进制
# 首次启动可能需要等待
```

### 问题 2: 端口冲突
**症状**: 无法启动 IPFS 节点

**解决**:
```bash
# 检查端口占用
lsof -i :5001

# 如果有其他 IPFS 实例（如 IPFS Desktop），请先关闭
```

### 问题 3: 消息发送失败
**症状**: 显示"消息发送失败"错误

**排查步骤**:
1. 检查 IPFS 节点是否运行
2. 检查网络连接
3. 查看浏览器 Console 错误日志
4. 查看错误提示框的具体错误信息

---

## 📊 验证检查清单

### 基础功能（必须全部通过）
- [ ] 群聊按钮正常工作
- [ ] 创建群聊功能可用
- [ ] 加入群聊功能可用
- [ ] 发送消息功能正常
- [ ] 接收消息功能正常
- [ ] 消息持久化正常
- [ ] 群聊切换正常

### UI/UX（应该通过）
- [ ] 错误提示清晰可见
- [ ] 加载状态明确
- [ ] 动画流畅无卡顿
- [ ] 暗色模式正常显示

### 性能（应该通过）
- [ ] 消息延迟 < 2 秒
- [ ] 内存使用稳定
- [ ] 无明显性能问题

### 安全性（必须通过）
- [ ] XSS 防护有效（尝试发送 `<script>alert('xss')</script>`，应被过滤）

---

## 📝 验证报告模板

```markdown
## 验证报告

**验证日期**: 2026-02-28
**验证人**: [您的名字]
**应用版本**: v0.1.10

### 测试结果

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 群聊按钮 | ✅/❌ | |
| 创建群聊 | ✅/❌ | |
| 加入群聊 | ✅/❌ | |
| 发送消息 | ✅/❌ | |
| 接收消息 | ✅/❌ | |
| 错误提示 | ✅/❌ | |
| 加载状态 | ✅/❌ | |
| XSS 防护 | ✅/❌ | |

### 发现的问题

1. [问题描述]
2. [问题描述]

### 总体评价

[通过/不通过]
```

---

## 🚀 验证通过后

如果所有测试都通过，可以准备发布：

```bash
# 1. 提交更改
git add .
git commit -m "feat: MVP 0.1.10 - 群聊功能完善和 Bug 修复"

# 2. 创建标签
git tag -a v0.1.10 -m "MVP Release v0.1.10 - Group Chat Improvements"

# 3. 推送
git push origin main
git push origin v0.1.10

# 4. 构建（可选）
npm run build:tauri:macos
```

---

**验证预计时间**: 30-60 分钟

**如有问题，请查看**:
- 浏览器 Console 日志
- 网络请求（Network 标签）
- 应用日志输出
