# Alou 桌面应用新手使用指南

> 📅 最后更新：2026 年 3 月  
> 📦 最新版本：v0.3.1  
> 👶 小白友好版 - 从零开始教你用 Alou

---

## 📖 什么是 Alou？

**Alou** 是一个可以让你和多个 AI 智能体一起聊天的桌面应用。

想象一下：你可以创建一个群聊，邀请几个不同特长的 AI（比如一个会编程的、一个会写作的、一个会画图的），然后让他们一起帮你完成任务！

### 你能用它做什么？

- 💬 **和 AI 聊天** - 像微信一样和 AI 对话
- 👥 **创建群聊** - 拉多个 AI 一起讨论问题
- 🧠 **让 AI 帮你工作** - 写代码、写文章、查资料
- 💾 **保存对话记录** - 聊天记录自动保存，随时查看

---

## 🚀 第一步：下载安装

### 1. 下载应用

打开浏览器，访问：https://github.com/logos-42/alou-rust/releases

找到最新版本（v0.3.1 或更高），根据你的电脑系统下载：

| 电脑系统 | 下载哪个 |
|----------|----------|
| Windows | `alou-desktop_x64-setup.exe` |
| Mac | `alou-desktop_x64.dmg` 或 `alou-desktop_aarch64.dmg`（M1/M2 芯片） |
| Linux | `alou-desktop_x86_64.AppImage` |

### 2. 安装应用

**Windows 用户**：
1. 双击下载的 `.exe` 文件
2. 点击"下一步"
3. 选择安装位置（或保持默认）
4. 点击"安装"
5. 安装完成后点击"完成"

**Mac 用户**：
1. 双击下载的 `.dmg` 文件
2. 把 Alou 图标拖到"Applications"文件夹
3. 在"应用程序"里找到 Alou，双击打开

**Linux 用户**：
1. 右键点击 `.AppImage` 文件 → 属性 → 权限
2. 勾选"允许作为程序执行文件"
3. 双击运行

---

## 🎯 第二步：第一次打开应用

### 1. 启动应用

- Windows：在桌面或开始菜单找到 "Alou" 图标，双击
- Mac：在"应用程序"里找到 "Alou"，双击
- Linux：在应用菜单找到 "Alou"，点击

### 2. 认识界面

打开后你会看到：

```
┌─────────────────────────────────────────────────────┐
│  Alou                                    [设置] [👤] │  ← 顶部栏
├──────────────┬──────────────────────────────────────┤
│              │                                      │
│  左侧边栏    │           主聊天区域                  │
│  - 我的智能体 │                                      │
│  - 群聊列表  │     （这里是聊天内容显示区域）         │
│  - 历史记录  │                                      │
│              │                                      │
│              │  ┌────────────────────────────────┐  │
│              │  │ 输入消息...              [发送] │  │  ← 底部输入框
└──────────────┴──────────────────────────────────────┘
```

**各部分作用**：

| 区域 | 作用 |
|------|------|
| 顶部栏 | 显示应用名称、设置按钮、你的头像 |
| 左侧边栏 | 切换不同的聊天（智能体、群聊、历史记录） |
| 主聊天区域 | 显示聊天内容 |
| 底部输入框 | 输入你想说的话 |

---

## 💬 第三步：开始第一次聊天

### 1. 选择一个智能体

在左侧边栏，点击 **"我的智能体"**，你会看到可用的 AI 列表：

- **Claude** - 擅长写作、分析、编程
- **DeepSeek** - 擅长中文对话、编程

点击其中一个（比如 Claude），主聊天区域就会打开对话窗口。

### 2. 发送第一条消息

在底部的输入框里打字，比如：

```
你好，请介绍一下你自己
```

然后点击右边的 **"发送"** 按钮（或直接按键盘的 `Enter` 键）。

### 3. 等待 AI 回复

AI 会在几秒钟内回复你。你会看到：
- 一个"正在输入..."的提示
- 然后 AI 的回复会逐字显示（像真人打字一样）

### 4. 继续对话

继续输入你的问题，比如：

```
能帮我写一首关于春天的诗吗？
```

AI 会根据上下文继续和你聊天。

---

## 👥 第四步：创建群聊（多个 AI 一起聊）

### 1. 点击"群聊"

在左侧边栏，找到 **"群聊"** 部分，点击。

### 2. 创建新群聊

点击 **"+ 创建群聊"** 按钮（或类似的加号图标）。

会弹出一个小窗口：

```
┌────────────────────────────┐
│     创建新群聊              │
│                            │
│  群聊名称：[ 输入名称... ]  │
│                            │
│      [取消]    [创建]       │
└────────────────────────────┘
```

- 在"群聊名称"里输入你想要的名字，比如"我的工作助手团"
- 点击 **"创建"** 按钮

### 3. 邀请 AI 加入

创建成功后，你可以邀请 AI 加入：

1. 点击群聊名称进入群聊
2. 点击右上角的 **"设置"** 或 **"成员管理"**
3. 点击 **"邀请成员"**
4. 从列表中选择你想要的 AI（可以选多个）
5. 点击 **"确认"**

### 4. 在群聊里发消息

现在你可以在群里发消息了，所有被邀请的 AI 都会看到并回复。

比如你可以问：

```
我想做一个个人网站，需要哪些步骤？
```

然后不同的 AI 会从不同角度给你建议。

---

## 📁 第五步：查看历史记录

### 1. 找到历史记录

在左侧边栏，点击 **"历史记录"**。

### 2. 浏览过去的对话

你会看到所有之前的对话列表，按时间排序。

点击任意一条，就可以查看当时的完整对话内容。

### 3. 继续之前的对话

如果想继续之前的话题：
1. 在历史记录里找到那条对话
2. 点击进入
3. 直接在底部输入框继续发消息

---

## ⚙️ 第六步：基本设置

### 1. 打开设置

点击右上角的 **"⚙️ 设置"** 图标（或齿轮图标）。

### 2. 常见设置项

| 设置项 | 作用 | 推荐设置 |
|--------|------|----------|
| 主题 | 切换亮色/暗色模式 | 根据喜好 |
| 通知 | 是否显示新消息提醒 | 开启 |
| 语言 | 界面语言 | 中文 |
| AI 模型 | 选择默认使用的 AI | Claude 或 DeepSeek |

### 3. 保存设置

修改后记得点击 **"保存"** 或 **"应用"** 按钮。

---

## 🎓 实用小技巧

### 技巧 1：快速切换聊天

- 点击左侧边栏的不同聊天，可以快速切换
- 最近聊天的会显示在顶部

### 技巧 2：使用快捷命令

在输入框输入 `/` 可以看到快捷命令：

- `/help` - 查看帮助
- `/clear` - 清空当前对话
- `/settings` - 打开设置

### 技巧 3：复制 AI 的回答

- 鼠标悬停在 AI 的回复上
- 会出现一个 **"复制"** 按钮
- 点击即可复制整段内容

### 技巧 4：给 AI 更清晰的指令

想让 AI 更好地帮你，可以这样说：

❌ 不太好：`帮我写代码`

✅ 更好：`帮我用 Python 写一个函数，输入一个数字列表，返回最大值和最小值`

### 技巧 5：让 AI 分步骤思考

复杂问题可以要求 AI 分步骤：

```
请一步步思考，帮我分析这个问题...
```

---

## ❓ 常见问题

### Q1: 应用打不开怎么办？

**试试这些**：
1. 检查电脑系统是否满足要求
2. 重新下载安装包
3. Windows 用户右键"以管理员身份运行"
4. Mac 用户去"系统偏好设置 → 安全性"里允许打开

### Q2: AI 不回复怎么办？

**检查这些**：
1. 网络连接是否正常
2. 是否配置了 API Key（如果需要）
3. 尝试刷新页面或重启应用

### Q3: 聊天记录会丢失吗？

**不会**！聊天记录会自动保存在本地，下次打开应用还能看到。

### Q4: 可以同时在多个群聊吗？

**可以**！你可以加入多个群聊，在左侧边栏切换。

### Q5: 需要付费吗？

Alou 应用本身是免费的。但如果使用某些 AI 服务（如 Claude API），可能需要向对应服务商付费。

---

## 🎉 开始使用吧！

现在你已经学会了 Alou 的基本使用方法：

1. ✅ 下载并安装应用
2. ✅ 打开应用，认识界面
3. ✅ 和单个 AI 聊天
4. ✅ 创建群聊，邀请多个 AI
5. ✅ 查看历史记录
6. ✅ 基本设置

**接下来**：
- 试着创建一个群聊，邀请 2-3 个 AI
- 让 AI 帮你完成一个实际任务（比如写一封邮件、规划旅行）
- 探索更多高级功能

---

**📞 需要帮助？**

- 查看完整文档：https://github.com/logos-42/alou-rust
- 提交问题：https://github.com/logos-42/alou-rust/issues

**祝你使用愉快！** 🎊
