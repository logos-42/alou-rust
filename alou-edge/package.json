{
  "name": "alou-edge",
  "version": "0.2.1",
  "description": "Alou Edge - Web3 AI Agent on Cloudflare Workers",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "clean": "node ./scripts/worker-build.js --clean-only",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "node ./scripts/worker-build.js",
    "build:cargo": "cargo build --target wasm32-unknown-unknown --release",
    "test": "cargo test"
  },
  "keywords": [
    "cloudflare-workers",
    "web3",
    "ai-agent",
    "blockchain"
  ],
  "author": "",
  "repository": {
    "type": "git",
    "url": "https://github.com/alou-pay/aloupay"
  },
  "license": "MIT OR Apache-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.53.0"
  }
}
