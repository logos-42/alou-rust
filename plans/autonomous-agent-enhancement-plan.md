# 自主智能体增强实施计划

## 实施状态追踪

**开始日期**: 2026-03-17
**当前阶段**: 阶段 1 - 智能感知层
**总体进度**: 0%

---

## 阶段 1: 智能感知层 (Perception Engine)

**目标**: 让 AI 在每次迭代时获取相关上下文，而非仅看当前对话

**状态**: 🟡 进行中

**任务清单**:
- [ ] 定义核心数据结构
  - [ ] `RetrievedContext`
  - [ ] `Intent` 枚举
  - [ ] `EnvironmentState` 扩展
- [ ] 创建 `PerceptionEngine`
- [ ] 实现意图识别
- [ ] 实现上下文检索
- [ ] 集成到 `Executor`
- [ ] 单元测试

**预计完成**: 2 天

---

## 阶段 2+4: Prompt 优化 + 反思层

**目标**: 改善 AI 决策质量，强制自我评估

**状态**: ⚪ 待开始

**任务清单**:
- [ ] 扩展 `Thought` 结构
- [ ] 添加 `Reflection` 字段
- [ ] 重构 `build_reasoning_prompt`
- [ ] 实现干预逻辑
- [ ] 集成测试

**预计完成**: 2 天

---

## 阶段 3: 元行动工具

**目标**: 让 AI 能主动获取信息、管理目标

**状态**: ⚪ 待开始

**任务清单**:
- [ ] 创建 `MetaActionTools`
- [ ] 实现 `memory_search`
- [ ] 实现 `read_document`
- [ ] 实现 `manage_goal`
- [ ] 实现 `remember`
- [ ] 注册到 `ToolRegistry`

**预计完成**: 2 天

---

## 阶段 5: 目标系统

**目标**: 支持长期目标追踪，跨 session 持续

**状态**: ⚪ 待开始

**任务清单**:
- [ ] 设计数据库 schema
- [ ] 创建 `GoalTracker`
- [ ] 实现 `GoalStorage` trait
- [ ] SQLite 存储实现
- [ ] 集成到感知层

**预计完成**: 2 天

---

## 验证标准

### 阶段 1
- [ ] "你好" → 只加载用户画像
- [ ] "优化这段代码" → 加载编码规范文档
- [ ] "继续上次的工作" → 加载活跃目标

### 阶段 2+4
- [ ] 简单问候直接 complete
- [ ] AI 必须评估信息充分性
- [ ] 低置信度时系统能自动干预

### 阶段 3
- [ ] AI 能调用 `memory_search`
- [ ] AI 能调用 `read_document`
- [ ] AI 能调用 `manage_goal`

### 阶段 5
- [ ] AI 能创建长期目标
- [ ] 跨 session 保持目标状态

---

## 技术债务

- 现有 `memory.rs` 需要重构
- `executor.rs` 的 perceive-reason-act 循环需要扩展
- 需要添加更多集成测试

---

## 风险与缓解

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 意图识别不准确 | 中 | 使用 LLM 辅助分类 + 关键词匹配 |
| 记忆检索性能 | 中 | 添加缓存层 + 限制检索数量 |
| Prompt 过长 | 低 | 动态摘要 + 优先级筛选 |
| 数据库迁移 | 低 | 使用 SQLite 迁移脚本 |

---

## 下一步行动

1. ✅ 创建 `RetrievedContext` 结构
2. ✅ 创建 `Intent` 枚举
3. ⏭️ 实现 `PerceptionEngine`
4. ⏭️ 集成到 `Executor`

---

**最后更新**: 2026-03-17
